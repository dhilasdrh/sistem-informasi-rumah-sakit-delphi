program rekammedis;

uses
  Forms,
  login in 'login.pas' {Form1},
  pasien in 'pasien.pas' {Form4},
  dokter in 'dokter.pas' {Form5},
  datamedis in 'datamedis.pas' {Form6},
  registrasi in 'registrasi.pas' {Form3},
  RawatJalan in 'RawatJalan.pas' {Form9},
  DaftarPasien in 'DaftarPasien.pas' {Form2},
  DaftarDokter in 'DaftarDokter.pas' {Form7};

{$R *.res}

begin
  Application.Initialize;         
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm4, Form4);
  Application.CreateForm(TForm5, Form5);
  Application.CreateForm(TForm6, Form6);
  Application.CreateForm(TForm3, Form3);
  Application.CreateForm(TForm9, Form9);
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm7, Form7);
  Application.Run;
end.
